# Data

`data/latest` is a small legacy fixture used by the original scaffold eval harness.
Paper-scale generated datasets should be written outside source control or committed
only as manifests and small reproducible samples.
